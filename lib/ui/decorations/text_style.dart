import 'package:flutter/material.dart';

class CustomTextStyle extends StatelessWidget {
  final double mSize;
  final Color mColor;
  const CustomTextStyle({Key? key,required this.mSize, required this.mColor}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
